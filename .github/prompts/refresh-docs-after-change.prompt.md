# Refresh Docs After Change

Use the relevant instruction files and templates to refresh documentation after a code, config, or deployment change.

Requirements:
- inspect the changed code first
- update only the impacted docs domains
- keep business, API, backend, and ops docs separate
- avoid duplicating existing content
- summarize what was updated and what was intentionally deferred
